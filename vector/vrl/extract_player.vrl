words = split!(.log_data, " ")
player = words[0]

joined_str = "left the game"
if .is_join == true {
  joined_str = "joined the game"
}

. = {}
.message = "{\"content\": \"" + player + " " + joined_str + "\"}"

